[project]
name = "jaffle-shop"
version = "3.0.0"
description = "dbt sample jaffle shop project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core==2.0.0rc5",
    "dbt-duckdb",
    "duckdb==1.5.5",
]

[tool.uv]
package = false

[tool.uv.sources]
dbt-duckdb = { git = "https://github.com/duckdb/dbt-duckdb.git" }